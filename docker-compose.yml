version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8084:8084"
    volumes:
      - ./backend/data:/app/data
    environment:
      - DATABASE_URL=sqlite:///data/school.db

  frontend:
    build: ./frontend
    ports:
      - "8083:80"
    depends_on:
      - backend
